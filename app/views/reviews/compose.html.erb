<h1>Write a Review for <%= @movie.name %></h1>

<%= form_with model: @review, url: reviews_path, local: true do |f| %>
  <%= f.hidden_field :movie_id, value: @movie.id %>

  <div class="mb-3">
    <%= f.label :rating %><br>
    <div class="star-rating d-flex flex-row-reverse justify-content-end">
      <% (1..5).reverse_each do |i| %>
        <%= f.radio_button :rating, i, id: "star#{i}", class: "d-none" %>
        <label for="star<%= i %>" class="star">&#9733;</label>
      <% end %>
    </div>
  </div>

  <div class="mb-3">
    <%= f.label :text, "Your Review" %>
    <%= f.text_area :text, rows: 4, class: "form-control" %>
  </div>

  <%= f.submit "Submit Review", class: "btn btn-primary" %>
<% end %>